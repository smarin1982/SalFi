---
phase: 13-multi-year-historical-pdf-ingestion
task: hotfix-session
status: in_progress
last_updated: 2026-03-20T09:20:16.828Z
---

<current_state>
Post-phase hotfix session. Phase 13 was marked complete but several bugs were found during
live testing with CROC (centro-radio-oncologico-del-caribe). Multiple fixes applied across
the backfill + validation pipeline. Context exhausted mid-session; user also asked about
GitHub remote (none configured — local commits only, no remote set up for smarin1982).
</current_state>

<completed_work>

- Fixed `LatamAgent.run()` Step 3b: inline historical backfill now reads `scraper_profiles.json`
  `historical_pdfs` before Playwright crawl (profile entries take precedence as validated URLs)
- Fixed `app.py` backfill block: same profile-first logic; also clears listing cache on
  "↺ Histórico" button press so stale session data doesn't block fresh profile reads
- Fixed `_render_backfill_status()`: was defined but never called — added call after backfill
  processing block; `not_found` shows "sin PDF disponible" without retry button
- Added 2020/2021/2022/2014 URLs to `scraper_profiles.json` for CROC (fetched from
  https://crocsas.com/Estados-financieros.html) — these were missing because year regex
  `\b2020\b` fails on filenames like `ESTADOSFINANCIEROSAO2020CROC.pdf` (no word boundary)
- Fixed `latam_backfiller._extract_year_from_text()`: added Pass 2 without `\b` for embedded
  years in all-caps filenames
- Fixed `latam_backfiller.run_year()`: was passing `self.raw_dir` to `_download_pdf` but that
  function appends `/raw/` internally → `raw/raw/` double path. Now passes `self.storage_path`
- Fixed `latam_validation.render_latam_validation_panel()`: expected dict but received
  `ExtractionResult` object. Added `_extraction_result_to_dict()` converter + auto-conversion
  at function entry
- Fixed `latam_validation`: field labels showed "USD" — now use `currency_code` from result
  (e.g. "Ingresos (COP)")
- Fixed `app.py`: `latam_pending_company` was stored as string slug — now stored as
  `{"slug": ..., "country": ...}` dict (two places: re-extract button + backfill block)
- Fixed `latam_validation._handle_confirm` + `_handle_discard`: neither advanced the backfill
  queue after confirm/discard → infinite loop re-processing same year. Added
  `_advance_backfill_queue(slug)` helper called by both handlers
- Fixed `latam_backfiller.py` SyntaxWarning: `\d` in docstring (non-raw string)

</completed_work>

<remaining_work>

- User has more feedback to deliver (was about to give new comments when context ran out)
- Verify end-to-end: CROC backfill completes (2020/2021/2022 confirmed or auto-written),
  all 5 years show in dashboard
- GitHub remote setup: user asked about smarin1982 — no remote configured yet. May want
  to add remote and push
- SyntaxWarning fix needs verification (docstring escape)
- OCR year inference for `ESTADOSFINANCIEROS20222021CROC.pdf` infers primary=2025/comp=2024
  instead of 2022/2021 — this is a data quality issue with the badly-scanned PDF. The
  backfiller falls back to results[0] which may have wrong fiscal_year. Not yet fixed.

</remaining_work>

<decisions_made>

- Profile `historical_pdfs` take precedence over fresh Playwright crawl (already validated)
- `not_found` years show label only, no retry button (no PDF = can't retry)
- Both confirm AND discard must advance the backfill queue (previously only discard comment
  said it would, but neither actually did)
- `latam_pending_company` now stored as dict (not string) everywhere it's set

</decisions_made>

<blockers>

- OCR year inference bug for CROC's 2021-2022 PDF: OCR reads 2025/2024 instead of 2022/2021.
  The `run_year(year=2021)` falls back to results[0] (wrong year). This causes validation
  panel to show for year 2021 repeatedly until user confirms — now fixed by queue advancement,
  but the underlying data written may have wrong fiscal_year. Needs investigation.

</blockers>

<context>
Working through live bugs found during CROC backfill testing. The main themes:
1. scraper_profiles.json was the ground truth for historical PDFs but wasn't being used
2. The validation panel had accumulated technical debt (wrong type, wrong currency labels,
   company stored as wrong type, queue never advanced)
3. No GitHub remote is configured — user asked about this, should be addressed next session

All changes are uncommitted (git status shows many modified files). No remote configured.
</context>

<next_action>
1. Ask user for their remaining feedback comments
2. Address GitHub remote setup if requested (git remote add origin https://github.com/smarin1982/...)
3. Verify CROC now loads 2020/2021/2022 after the queue-advancement fix
4. Commit all accumulated fixes with descriptive message
</next_action>
