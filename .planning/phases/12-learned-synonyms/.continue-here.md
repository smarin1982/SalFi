---
phase: 12-learned-synonyms
task: 0
total_tasks: 5
status: planning_complete
last_updated: 2026-03-10T15:26:01.542Z
---

<current_state>
Phase 12 planning is COMPLETE. All 5 plans written. No code has been executed yet.
The user wants to execute Phase 12 when ready. Currently validating PDF reading in the dashboard.

Active bug fixes made this session (NOT in any phase plan — applied directly to main):
- app.py: _run_latam_pipeline now accepts force_refresh param; upload path passes force_refresh=True
- app.py: _render_latam_kpi_cards shows all available non-NaN KPIs (not just S&P 500 defaults)
- app.py: fixed `with col:` TypeError when n==1 (was using `st` module as context manager)
- LatamAgent.py: detects local PDF path and skips scraper (Step 1 bypass)
- LatamAgent.py: parquets_exist check before skip-scrape logic
- latam_concept_map.py: validate_tesseract() now loads dotenv + sets pytesseract.tesseract_cmd
- latam_concept_map.py: added "efectivo y equivalentes en efectivo" to cash synonyms
- latam_concept_map.py: added "cuentas comerciales - por cobrar" variants to receivables
- latam_concept_map.py: added "obligaciones financieras" to short_term_debt
- latam_extractor.py: Strategy 2 — regex requires thousand-separator format (avoids note refs)
- latam_extractor.py: excludes "no corrientes" labels to prevent total_assets false match
- latam_processor.py: returns fiscal_years list (was returning singular fiscal_year)
- app.py: fixed _latam_confidence_badge to read financials.parquet for critical fields check
</current_state>

<completed_work>

## Phase 12 Plans Created
- 12-01-PLAN.md: Candidate capture (latam_extractor unmatched → learned_candidates.jsonl) + concept map loader (learned_synonyms.json)
- 12-02-PLAN.md: Claude Haiku suggestion engine (latam_synonym_reviewer.py)
- 12-03-PLAN.md: Streamlit "Terminología Aprendida" review panel (app.py)
- 12-04-PLAN.md: Multi-year extraction from comparative PDFs (2-column OCR capture + _infer_fiscal_years)
- 12-05-PLAN.md: Financial summary table (5 key metrics) + working_capital local currency fix

## Session bug fixes (all committed or uncommitted — check git status)
All items listed in current_state above.
</completed_work>

<remaining_work>

- Execute 12-01: Wire unmatched-label capture + load learned_synonyms.json
- Execute 12-02: latam_synonym_reviewer.py with Claude Haiku
- Execute 12-03: Streamlit review panel
- Execute 12-04: Multi-year OCR extraction (2 columns → 2 ExtractionResult objects)
- Execute 12-05: Financial summary table + working_capital currency fix

Wave order: 12-01 → 12-02 → 12-03 → 12-04 → 12-05
12-04 and 12-05 can run in parallel after 12-03 completes.
</remaining_work>

<decisions_made>

- Learned synonyms: semi-supervised (capture automatic, mapping needs human approval) — prevents silent mis-mappings corrupting all future analyses
- learned_synonyms.json stored in data/latam/ (not git-tracked) — latam_concept_map.py (base, curated) stays in git
- Claude Haiku for suggestions (speed > depth for low-stakes synonym mapping)
- Multi-year: _infer_fiscal_years() scans first 500 chars of OCR for year pattern like "2024 Y 2023"
- Multi-year: extract() returns List[ExtractionResult]; latam_processor accepts list (backwards compat via isinstance check)
- Financial table: uses financials.parquet (raw USD) × fx_rate when Moneda Original toggle active
- working_capital fix: _format_latam_kpi_value() applies reverse FX for currency-type KPIs
- LATAM KPI display: show all available non-NaN KPIs when <2 sidebar selections have data
</decisions_made>

<blockers>

- MiRed IPS PDF: "Ganancia antes de impuesto" and "Ganancia o Perdida del año" are not in concept map → operating_income and net_income may be NaN after Phase 12-01 seeds are applied (12-01 seeds these directly in learned_synonyms.json)
- working_capital shows in USD regardless of toggle — fixed in 12-05 plan
- Only 1 year loads from comparative PDF — fixed in 12-04 plan
- fiscal_year hardcoded to datetime.now().year-1 (=2025) in LatamAgent — should be inferred from PDF; also addressed in 12-04 (_infer_fiscal_years)
</blockers>

<context>
The pipeline is now functional end-to-end for LATAM PDFs. Tesseract OCR works, extracts 9 KPIs
from a scanned Colombian IPS financial statement with Alta confidence. The dashboard renders
correctly. The main remaining gaps are:
1. More terms need to be mapped (Phase 12-01/02/03 — the learning system)
2. Both years from comparative PDFs should load (Phase 12-04)
3. Financial summary table + currency display fixes (Phase 12-05)

The user will signal when to start executing Phase 12.
Key file: data/latam/co/mired-ips/raw/ESTADOS-FINANCIEROS-A-CORTE-DE-DICIEMBRE-31-DEL-2024.pdf
is the test PDF — 5 pages, scanned, Colombian IPS, IFRS format, COP currency.
</context>

<next_action>
Run: /gsd:execute-phase 12
Then verify with the MiRed IPS PDF that:
1. learned_candidates.jsonl is written after extraction
2. Operating_income extracts after seeded synonyms load
3. Both 2024 and 2023 rows appear in financials.parquet
4. Financial summary table shows in dashboard
5. Working capital shows in COP when Moneda Original selected
</next_action>
