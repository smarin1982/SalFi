---
phase: 12-learned-synonyms
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - latam_extractor.py
  - latam_synonym_reviewer.py
autonomous: true
requirements:
  - SYN-03
gap_closure: true

must_haves:
  truths:
    - "Labels matching r'^\\d{4}$' (year column headers like '2020', '2021') are never written to learned_candidates.jsonl"
    - "Aggregate stop-words used standalone ('Total', 'Subtotal', 'TOTAL', 'Suma', 'Neto') are never written to learned_candidates.jsonl"
    - "get_review_candidates() excludes noise labels already present in the file — existing 146-record file is cleaned at read time"
    - "Actionable financial terms like 'Ingresos por actividades ordinarias' still pass through to learned_candidates.jsonl"
  artifacts:
    - path: "latam_extractor.py"
      provides: "Noise-filtered _append_candidate() — write-time guard for years + stop-words"
      contains: "_CANDIDATE_YEAR_RE"
    - path: "latam_synonym_reviewer.py"
      provides: "Read-time noise filter in get_review_candidates() — cleans existing noisy data"
      contains: "_is_noise_label"
  key_links:
    - from: "latam_extractor._append_candidate()"
      to: "data/latam/learned_candidates.jsonl"
      via: "early return when label matches _CANDIDATE_YEAR_RE or _CANDIDATE_STOP_WORDS"
      pattern: "_CANDIDATE_YEAR_RE|_CANDIDATE_STOP_WORDS"
    - from: "latam_synonym_reviewer.get_review_candidates()"
      to: "UI candidate list"
      via: "_is_noise_label() filter applied after reading JSONL"
      pattern: "_is_noise_label"
---

<objective>
Fix noise accumulation in the learned synonym candidate queue by filtering year-pattern labels and
aggregate stop-words at both write time (preventing future accumulation) and read time (cleaning
existing 146-record noisy file for backwards compatibility).

Purpose: The "Terminologia Aprendida" review panel currently shows 140 candidates at min_seen=2,
dominated by '2020', '2021', 'Total' (seen 12x) — these bury actionable financial terms and make
the panel nearly unusable. This fix closes the SYN-03 verification gap by restoring signal-to-noise
ratio in the review queue without deleting historical data.

Output: Modified latam_extractor.py (_append_candidate with guards) and latam_synonym_reviewer.py
(get_review_candidates with read-time filter). The learned_candidates.jsonl file is NOT modified —
noise is excluded at read time, preserving raw data integrity.
</objective>

<execution_context>
@C:/Users/Seb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Seb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-learned-synonyms/12-VERIFICATION.md

Key source files to read before editing:
- latam_extractor.py: locate _append_candidate() function (line ~216 per VERIFICATION.md) — read
  the full function to understand current guards before adding new ones
- latam_synonym_reviewer.py: locate get_review_candidates() — read the full function to understand
  how it reads from learned_candidates.jsonl and applies existing exclusions

Gap context (from 12-VERIFICATION.md):
- Root cause: _append_candidate() only guards len(label) >= 4 — allows '2020', '2021', 'Total'
- Noise candidates at min_seen=2: 'Total' (12x), '2020'/'2021'/'2022'/'2023' (8x each)
- Fix approach: write-time filter + read-time filter (Option A + B combined per gap_context)
- Noise patterns to filter:
  1. r'^\d{4}$' — exactly 4 digits (year column headers from comparative statements)
  2. Standalone stop-words (case-insensitive whole-string match): total, subtotal, suma, neto
     Note: 'neto' standalone only — "ingreso neto" should still pass through
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add write-time noise filter to _append_candidate() in latam_extractor.py</name>
  <files>latam_extractor.py</files>
  <action>
    Read latam_extractor.py fully before editing. Locate _append_candidate() (around line 216).

    Add two module-level constants immediately after the existing _CANDIDATES_FILE constant
    (do NOT place inside the function — module-level for performance and testability):

    ```python
    import re as _re

    # Labels to exclude from candidate capture — year column headers and aggregate totals
    _CANDIDATE_YEAR_RE = _re.compile(r"^\d{4}$")
    _CANDIDATE_STOP_WORDS = frozenset({"total", "subtotal", "suma", "neto"})
    ```

    Note: `import re as _re` uses a private alias to avoid polluting the module namespace.
    If `re` is already imported at the top of the file, add only the two constants (no
    duplicate import). Check the existing imports first.

    Inside _append_candidate(), add an early-return guard at the very top of the function body
    (after the docstring, BEFORE the try block — this is a semantic guard, not an I/O operation):

    ```python
    # Skip noise labels: year-only strings ('2020') and aggregate stop-words ('Total')
    _label_stripped = label.strip()
    if not _label_stripped or len(_label_stripped) < 4:
        return  # already guarded by callers but belt-and-suspenders
    if _CANDIDATE_YEAR_RE.match(_label_stripped):
        return  # column header like '2020', '2021'
    if _label_stripped.lower() in _CANDIDATE_STOP_WORDS:
        return  # aggregate row label like 'Total', 'Subtotal'
    ```

    Do NOT remove the existing try/except wrapper or any existing logic inside the function.
    The guard sits BEFORE the try block as a fast path — no I/O is attempted for noise labels.

    Verify the placement by re-reading the function after editing: the structure should be:
    1. Docstring
    2. Noise guard (new — returns early for years and stop-words)
    3. try: block (existing — file I/O, dedup logic)
    4. except: block (existing — non-blocking error handling)
  </action>
  <verify>
    <automated>cd "C:/Users/Seb/AI 2026" &amp;&amp; python -c "
import re
from latam_extractor import _append_candidate, _CANDIDATE_YEAR_RE, _CANDIDATE_STOP_WORDS
from pathlib import Path
import json, tempfile, os

# Confirm constants exist and are correct
assert _CANDIDATE_YEAR_RE.match('2020'), 'Year RE should match 2020'
assert _CANDIDATE_YEAR_RE.match('2023'), 'Year RE should match 2023'
assert not _CANDIDATE_YEAR_RE.match('2020a'), 'Year RE should not match 2020a'
assert not _CANDIDATE_YEAR_RE.match('income'), 'Year RE should not match income'
assert 'total' in _CANDIDATE_STOP_WORDS
assert 'subtotal' in _CANDIDATE_STOP_WORDS
assert 'suma' in _CANDIDATE_STOP_WORDS
assert 'neto' in _CANDIDATE_STOP_WORDS
print('Constants OK')

# Use a temp file to isolate test from real data
import latam_extractor as le
original = le._CANDIDATES_FILE
tmp = Path(tempfile.mktemp(suffix='.jsonl'))
le._CANDIDATES_FILE = tmp

try:
    # Noise labels should NOT be written
    _append_candidate('2020', 133279048.70, 1, 'header', 'test-co', 'CO', 'test.pdf')
    _append_candidate('2021', 120000000.00, 1, 'header', 'test-co', 'CO', 'test.pdf')
    _append_candidate('Total', 999999.00, 5, 'footer', 'test-co', 'CO', 'test.pdf')
    _append_candidate('TOTAL', 999999.00, 5, 'footer', 'test-co', 'CO', 'test.pdf')
    _append_candidate('Subtotal', 500000.00, 5, 'footer', 'test-co', 'CO', 'test.pdf')
    _append_candidate('Suma', 500000.00, 5, 'footer', 'test-co', 'CO', 'test.pdf')
    _append_candidate('Neto', 500000.00, 5, 'footer', 'test-co', 'CO', 'test.pdf')

    if tmp.exists():
        records = [json.loads(l) for l in tmp.read_text(encoding='utf-8').splitlines() if l.strip()]
        assert len(records) == 0, f'Noise labels should not be written, got {len(records)} records: {[r[\"label\"] for r in records]}'
    print('Noise filter PASS: year and stop-word labels not written')

    # Legitimate labels SHOULD be written
    _append_candidate('Ingresos por actividades ordinarias', 152000000000.0, 3, 'p_and_l', 'test-co', 'CO', 'test.pdf')
    _append_candidate('Gastos de administracion', 8000000000.0, 4, 'p_and_l', 'test-co', 'CO', 'test.pdf')
    _append_candidate('ingreso neto', 12000000000.0, 5, 'p_and_l', 'test-co', 'CO', 'test.pdf')  # 'neto' in phrase, not standalone

    records = [json.loads(l) for l in tmp.read_text(encoding='utf-8').splitlines() if l.strip()]
    assert len(records) == 3, f'Expected 3 legit records, got {len(records)}: {[r[\"label\"] for r in records]}'
    print('Legitimate labels PASS: 3 financial terms written correctly')

finally:
    le._CANDIDATES_FILE = original
    if tmp.exists():
        tmp.unlink()

print('ALL WRITE-TIME FILTER TESTS PASSED')
"
    </automated>
    <manual>Confirm _CANDIDATE_YEAR_RE and _CANDIDATE_STOP_WORDS are module-level constants in latam_extractor.py (not inside _append_candidate body)</manual>
  </verify>
  <done>
    - _CANDIDATE_YEAR_RE = re.compile(r"^\d{4}$") defined at module level in latam_extractor.py
    - _CANDIDATE_STOP_WORDS = frozenset({"total", "subtotal", "suma", "neto"}) defined at module level
    - _append_candidate() returns early for year-pattern labels and standalone stop-words
    - "ingreso neto" (phrase containing 'neto') still passes through — only standalone 'neto' is blocked
    - All automated assertions pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Add read-time noise filter to get_review_candidates() in latam_synonym_reviewer.py</name>
  <files>latam_synonym_reviewer.py</files>
  <action>
    Read latam_synonym_reviewer.py fully before editing. Locate get_review_candidates() — the function
    that reads learned_candidates.jsonl and returns CandidateRecord objects for the UI.

    Add a module-level helper function (place it before get_review_candidates()):

    ```python
    import re as _re_reviewer

    _NOISE_YEAR_RE = _re_reviewer.compile(r"^\d{4}$")
    _NOISE_STOP_WORDS = frozenset({"total", "subtotal", "suma", "neto"})


    def _is_noise_label(label: str) -> bool:
        """Return True if label is a noise entry (year header or aggregate stop-word).

        Used to filter existing learned_candidates.jsonl records that were captured
        before the write-time filter was added — backwards compatibility clean-up.
        """
        stripped = label.strip()
        if not stripped or len(stripped) < 4:
            return True
        if _NOISE_YEAR_RE.match(stripped):
            return True
        if stripped.lower() in _NOISE_STOP_WORDS:
            return True
        return False
    ```

    Note: If `re` is already imported at the top of latam_synonym_reviewer.py, use that import
    instead of `import re as _re_reviewer` — check existing imports first and reuse the name.
    Add only constants + function if `re` already present.

    Inside get_review_candidates(), after reading each candidate from the JSONL file and BEFORE
    adding it to the candidates list (but AFTER the existing exclusion checks for approved/rejected
    labels), add the noise filter:

    ```python
    # Skip noise labels: year column headers and standalone aggregate stop-words
    if _is_noise_label(record.get("label", "")):
        continue
    ```

    The placement must respect the existing exclusion logic order:
    1. Existing: skip if label is in learned_synonyms.json (approved or rejected) — keep first
    2. NEW: skip if _is_noise_label() — add after existing exclusions
    3. Existing: apply min_seen threshold filter — keep last before appending

    This order is intentional: if someone accidentally approved a noise label, the approved-exclusion
    check removes it from review (it's already handled). The noise filter is a second line of defense.

    Do NOT modify the CandidateRecord dataclass, the min_seen parameter, or any other logic.
  </action>
  <verify>
    <automated>cd "C:/Users/Seb/AI 2026" &amp;&amp; python -c "
from latam_synonym_reviewer import _is_noise_label, get_review_candidates

# Test _is_noise_label directly
assert _is_noise_label('2020') == True, '2020 should be noise'
assert _is_noise_label('2021') == True, '2021 should be noise'
assert _is_noise_label('2023') == True, '2023 should be noise'
assert _is_noise_label('Total') == True, 'Total should be noise'
assert _is_noise_label('TOTAL') == True, 'TOTAL should be noise'
assert _is_noise_label('Subtotal') == True, 'Subtotal should be noise'
assert _is_noise_label('Suma') == True, 'Suma should be noise'
assert _is_noise_label('Neto') == True, 'standalone Neto should be noise'
assert _is_noise_label('') == True, 'empty should be noise'
assert _is_noise_label('abc') == True, 'len<4 should be noise'

assert _is_noise_label('Ingresos operacionales') == False, 'financial term should not be noise'
assert _is_noise_label('Gastos de administracion') == False, 'financial term should not be noise'
assert _is_noise_label('ingreso neto') == False, 'phrase with neto should not be noise'
assert _is_noise_label('Total activos') == False, 'Total activos is not standalone stop-word'
assert _is_noise_label('Subtotal operativo') == False, 'Subtotal operativo is not standalone'
print('_is_noise_label ALL ASSERTIONS PASSED')

# Test get_review_candidates filters noise from existing file
# The existing learned_candidates.jsonl has noise: run with min_seen=1 and check no noise in output
candidates = get_review_candidates(min_seen=1)
noise_labels = {'2020', '2021', '2022', '2023', '2024', 'total', 'subtotal', 'suma', 'neto'}
for c in candidates:
    label_lower = c.label.strip().lower()
    import re
    assert not re.match(r'^\d{4}$', label_lower), f'Year label leaked through: {c.label}'
    assert label_lower not in noise_labels, f'Stop-word leaked through: {c.label}'
print(f'get_review_candidates filtered correctly: {len(candidates)} candidates (was 140+ before fix)')
print('ALL READ-TIME FILTER TESTS PASSED')
"
    </automated>
  </verify>
  <done>
    - _is_noise_label() helper function added to latam_synonym_reviewer.py
    - _NOISE_YEAR_RE and _NOISE_STOP_WORDS module-level constants defined
    - get_review_candidates() skips records where _is_noise_label(label) is True
    - Existing learned_candidates.jsonl with 146 records (including noise) returns clean list at read time
    - get_review_candidates(min_seen=1) returns zero year-string or standalone stop-word candidates
    - Total activos / Subtotal operativo / ingreso neto (phrases) still pass through
    - All automated assertions pass
  </done>
</task>

</tasks>

<verification>
cd "C:/Users/Seb/AI 2026" &amp;&amp; python -m py_compile latam_extractor.py &amp;&amp; python -m py_compile latam_synonym_reviewer.py &amp;&amp; echo "Syntax OK"
</verification>

<success_criteria>
- _append_candidate() in latam_extractor.py: year-pattern labels and standalone stop-words return early without I/O
- get_review_candidates() in latam_synonym_reviewer.py: _is_noise_label() filter applied to all candidates
- Running get_review_candidates(min_seen=1) against the existing 146-record file returns zero '2020'/'Total' entries
- Legitimate financial terms ('Ingresos por actividades ordinarias', 'ingreso neto', 'Total activos') still pass through
- Both py_compile checks pass (no syntax errors introduced)
- SYN-03 gap closed: candidate queue shows actionable financial terminology, not column headers
</success_criteria>

<output>
After completion, create `.planning/phases/12-learned-synonyms/12-07-SUMMARY.md`
</output>
