---
phase: 01-data-extraction
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - scraper.py
autonomous: true
requirements:
  - XTRCT-01
  - XTRCT-02
  - XTRCT-03
  - XTRCT-04

must_haves:
  truths:
    - "Running `python scraper.py AAPL` produces data/raw/AAPL/facts.json containing 10+ years of 10-K XBRL facts"
    - "Running the scraper a second time for the same ticker skips the SEC network call and returns immediately (cache hit)"
    - "The scraper resolves any S&P 500 ticker to CIK using data/cache/tickers.json without a per-resolution network call once tickers.json exists"
    - "The scraper never exceeds 10 req/s to SEC EDGAR (set_rate_limit(8) enforced)"
    - "Running `python scraper.py BRK.B` works (special ticker normalization)"
  artifacts:
    - path: "C:/Users/Seb/AI 2026/scraper.py"
      provides: "Complete Phase 1 scraper — ticker→CIK resolution, rate-limited XBRL download, raw JSON persistence"
      min_lines: 120
      exports:
        - scrape
        - build_ticker_map
        - resolve_cik
        - fetch_companyfacts
        - download_facts
        - validate_facts
  key_links:
    - from: "scraper.py _init_edgar()"
      to: "set_identity() + set_rate_limit(8)"
      via: "edgar library"
      pattern: "set_rate_limit\\(8\\)"
    - from: "scraper.py build_ticker_map()"
      to: "data/cache/tickers.json"
      via: "httpx.get(TICKERS_URL)"
      pattern: "tickers.json.*exists"
    - from: "scraper.py download_facts()"
      to: "data/raw/{TICKER}/facts.json"
      via: "out_path.exists() check"
      pattern: "out_path.exists\\(\\).*force_refresh"
---

<objective>
Implement scraper.py — the complete Phase 1 data extraction module.

Purpose: This is the sole deliverable of Phase 1. All subsequent phases (processor, agent, dashboard) depend on data/raw/{TICKER}/facts.json existing. scraper.py is the only code that talks to SEC EDGAR.
Output: C:/Users/Seb/AI 2026/scraper.py with full implementation of all 4 XTRCT requirements.
</objective>

<execution_context>
@C:/Users/Seb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Seb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-extraction/01-RESEARCH.md
@.planning/phases/01-data-extraction/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement scraper.py with all XTRCT functions</name>
  <files>C:/Users/Seb/AI 2026/scraper.py</files>
  <action>
Create `C:/Users/Seb/AI 2026/scraper.py` with the complete implementation below. This is the full file — write it verbatim.

Key decisions from research:
- Use `set_rate_limit(8)` (not 9 or 10) for a conservative safety margin under SEC's 10 req/s limit (XTRCT-02)
- Use direct `httpx.get()` for raw companyfacts JSON — NOT `Company.get_facts()` — to guarantee verbatim storage (XTRCT-04)
- Check `out_path.exists()` before any SEC call — local copy takes precedence (XTRCT-04)
- BRK.B: try both "BRK.B" and "BRK-B" on KeyError — SEC tickers.json may use either notation
- Call `_init_edgar()` at module-level so imports automatically configure identity
- `HEADERS` dict is populated by `_init_edgar()` and used for all direct httpx calls

```python
"""
scraper.py — Phase 1: Data Extraction
Sole responsibility: Talk to SEC EDGAR. Fetch, rate-limit, persist raw facts.json.
Nothing else. No KPI calculation, no Parquet, no Streamlit.
"""

import json
import os
import sys
from pathlib import Path

import httpx
from dotenv import load_dotenv
from edgar import set_identity, set_rate_limit
from loguru import logger
from tenacity import (
    retry,
    retry_if_exception_type,
    stop_after_attempt,
    wait_exponential,
)

# ---------------------------------------------------------------------------
# Constants
# ---------------------------------------------------------------------------

TICKERS_URL = "https://www.sec.gov/files/company_tickers.json"
COMPANYFACTS_URL = "https://data.sec.gov/api/xbrl/companyfacts/CIK{cik}.json"
DATA_DIR = Path(__file__).parent / "data"

# Populated by _init_edgar() at module load
HEADERS: dict[str, str] = {}


# ---------------------------------------------------------------------------
# Initialization — MUST run before any SEC API call
# ---------------------------------------------------------------------------

def _init_edgar() -> None:
    """
    Configure edgartools identity and rate limit.
    Called once at module load. Sets User-Agent header for all direct httpx calls.
    XTRCT-02: rate limit set to 8 req/s (conservatively under SEC's 10 req/s policy).
    """
    load_dotenv(Path(__file__).parent / ".env")
    identity = os.getenv("EDGAR_IDENTITY")
    if not identity:
        raise EnvironmentError(
            "EDGAR_IDENTITY not set. Add to .env: EDGAR_IDENTITY='Name email@domain.com'"
        )
    set_identity(identity)
    set_rate_limit(8)  # XTRCT-02: stay conservatively under 10 req/s
    HEADERS["User-Agent"] = identity
    logger.debug(f"edgartools initialized — identity: {identity[:30]}... rate_limit: 8 req/s")


# ---------------------------------------------------------------------------
# XTRCT-01: Ticker → CIK resolution with local cache
# ---------------------------------------------------------------------------

def build_ticker_map(cache_path: Path | None = None) -> dict[str, str]:
    """
    Download company_tickers.json from SEC (once) and build {TICKER: CIK} map.
    Returns {ticker_upper: zero_padded_10d_cik_string}.
    Reads from cache_path if file exists — zero network calls per resolution.
    XTRCT-01: downloads at startup, caches locally.
    """
    if cache_path is None:
        cache_path = DATA_DIR / "cache" / "tickers.json"

    if cache_path.exists():
        logger.debug(f"Loading ticker map from cache: {cache_path}")
        raw = json.loads(cache_path.read_text(encoding="utf-8"))
    else:
        logger.info(f"Downloading ticker→CIK map from SEC: {TICKERS_URL}")
        cache_path.parent.mkdir(parents=True, exist_ok=True)
        resp = httpx.get(TICKERS_URL, headers=HEADERS, timeout=30)
        resp.raise_for_status()
        raw = resp.json()
        cache_path.write_text(json.dumps(raw), encoding="utf-8")
        logger.info(f"Ticker map cached: {cache_path} ({len(raw)} entries)")

    # Structure: {"0": {"cik_str": 320193, "ticker": "AAPL", "title": "..."}, ...}
    return {v["ticker"].upper(): str(v["cik_str"]).zfill(10) for v in raw.values()}


def resolve_cik(ticker: str, ticker_map: dict[str, str]) -> str:
    """
    Resolve ticker to zero-padded 10-digit CIK string.
    Handles BRK.B / BRK-B normalization (SEC may use either dot or dash).
    Raises ValueError for unknown tickers.
    """
    normalized = ticker.upper()
    cik = ticker_map.get(normalized)

    if cik is None:
        # BRK.B edge case: try alternative notation
        alt = normalized.replace(".", "-") if "." in normalized else normalized.replace("-", ".")
        cik = ticker_map.get(alt)
        if cik is not None:
            logger.debug(f"Ticker '{ticker}' resolved via alternative notation '{alt}' → CIK {cik}")
        else:
            raise ValueError(
                f"Ticker '{ticker}' not found in SEC tickers.json. "
                f"Verify it is a valid S&P 500 ticker (tried '{normalized}' and '{alt}')."
            )

    return cik


# ---------------------------------------------------------------------------
# XTRCT-03: Raw companyfacts JSON download with retry
# XTRCT-04: Existence check — use local copy if already present
# ---------------------------------------------------------------------------

@retry(
    stop=stop_after_attempt(5),
    wait=wait_exponential(multiplier=2, min=2, max=60),
    retry=retry_if_exception_type((httpx.HTTPStatusError, httpx.TransportError)),
    reraise=True,
)
def fetch_companyfacts(cik: str) -> dict:
    """
    Download raw XBRL company facts JSON from SEC EDGAR companyfacts endpoint.
    cik: zero-padded 10-digit string, e.g. "0000320193"
    Returns verbatim parsed JSON dict.
    Retries up to 5 times with exponential backoff on HTTP/transport errors.
    Does NOT retry 404 (deterministic failure — no XBRL data for this CIK).
    XTRCT-03: uses official EDGAR XBRL companyfacts endpoint.
    """
    url = COMPANYFACTS_URL.format(cik=cik)
    logger.debug(f"Fetching companyfacts: {url}")

    with httpx.Client(timeout=60.0) as client:
        resp = client.get(url, headers=HEADERS)

    if resp.status_code == 404:
        raise ValueError(
            f"No XBRL companyfacts found for CIK {cik}. "
            f"Company may not file XBRL (foreign filer or pre-XBRL era), or CIK is incorrect."
        )
    resp.raise_for_status()
    return resp.json()


def download_facts(
    ticker: str,
    cik: str,
    force_refresh: bool = False,
) -> Path:
    """
    Download and persist facts.json for a ticker to data/raw/{TICKER}/facts.json.
    Skips download if file already exists and force_refresh is False.
    Returns path to facts.json.
    XTRCT-04: local copy takes precedence over re-fetching.
    """
    out_path = DATA_DIR / "raw" / ticker.upper() / "facts.json"

    if out_path.exists() and not force_refresh:
        logger.info(f"Using cached facts.json for {ticker}: {out_path}")
        return out_path  # XTRCT-04: use local copy

    out_path.parent.mkdir(parents=True, exist_ok=True)
    logger.info(f"Downloading facts for {ticker} (CIK {cik})...")
    facts = fetch_companyfacts(cik)
    out_path.write_text(json.dumps(facts, indent=2), encoding="utf-8")
    size_kb = out_path.stat().st_size // 1024
    logger.info(f"Facts saved: {out_path} ({size_kb} KB)")
    return out_path


# ---------------------------------------------------------------------------
# Validation — smoke-test after download
# ---------------------------------------------------------------------------

def validate_facts(facts_path: Path) -> dict:
    """
    Smoke-test a downloaded facts.json for usable 10-K data.
    Returns {"ok": bool, "fy_count": int, "earliest_fy": int, "latest_fy": int}
    Does NOT hit SEC — reads local file only.
    """
    facts = json.loads(facts_path.read_text(encoding="utf-8"))
    us_gaap = facts.get("facts", {}).get("us-gaap", {})

    if not us_gaap:
        return {"ok": False, "fy_count": 0, "reason": "No us-gaap facts found in JSON"}

    all_fy: set[int] = set()
    for concept_data in us_gaap.values():
        for unit_entries in concept_data.get("units", {}).values():
            for entry in unit_entries:
                if entry.get("form") == "10-K" and entry.get("fp") == "FY":
                    fy = entry.get("fy")
                    if fy is not None:
                        all_fy.add(fy)

    if not all_fy:
        return {"ok": False, "fy_count": 0, "reason": "No 10-K FY entries in us-gaap"}

    return {
        "ok": True,
        "fy_count": len(all_fy),
        "earliest_fy": min(all_fy),
        "latest_fy": max(all_fy),
        "entity": facts.get("entityName", "unknown"),
    }


# ---------------------------------------------------------------------------
# Main entry point
# XTRCT-01+02+03+04: orchestrates all steps for a single ticker
# ---------------------------------------------------------------------------

def scrape(ticker: str, force_refresh: bool = False) -> Path:
    """
    Main entry point for Phase 1.
    Resolves ticker → CIK → downloads facts.json (or uses cached copy).
    Returns path to data/raw/{TICKER}/facts.json.
    Raises ValueError for invalid tickers or companies with no XBRL data.
    """
    ticker = ticker.upper()
    logger.info(f"Scraping {ticker} (force_refresh={force_refresh})")

    ticker_map = build_ticker_map()  # loads from cache if exists (XTRCT-01)
    cik = resolve_cik(ticker, ticker_map)
    logger.debug(f"{ticker} → CIK {cik}")

    facts_path = download_facts(ticker, cik, force_refresh=force_refresh)

    validation = validate_facts(facts_path)
    if validation["ok"]:
        logger.info(
            f"{ticker} ({validation['entity']}): "
            f"{validation['fy_count']} fiscal years available "
            f"({validation['earliest_fy']}–{validation['latest_fy']})"
        )
    else:
        logger.warning(f"{ticker}: facts.json validation warning — {validation.get('reason')}")

    return facts_path


# ---------------------------------------------------------------------------
# Module initialization — runs on import
# ---------------------------------------------------------------------------

_init_edgar()


# ---------------------------------------------------------------------------
# CLI usage: python scraper.py AAPL [--force]
# ---------------------------------------------------------------------------

if __name__ == "__main__":
    if len(sys.argv) < 2:
        print("Usage: python scraper.py TICKER [--force]")
        print("Example: python scraper.py AAPL")
        print("         python scraper.py BRK.B --force")
        sys.exit(1)

    ticker_arg = sys.argv[1]
    force_arg = "--force" in sys.argv

    try:
        path = scrape(ticker_arg, force_refresh=force_arg)
        print(f"Success: {path}")
    except ValueError as e:
        logger.error(str(e))
        sys.exit(1)
```
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'C:/Users/Seb/AI 2026'); import scraper; print('Module loads OK'); print('scrape callable:', callable(scraper.scrape)); print('build_ticker_map callable:', callable(scraper.build_ticker_map))" 2>&1</automated>
    <manual>Output shows "Module loads OK" and both callables print True. If EDGAR_IDENTITY error appears, .env file is missing or not found.</manual>
  </verify>
  <done>scraper.py imports without error; all 6 public functions (scrape, build_ticker_map, resolve_cik, fetch_companyfacts, download_facts, validate_facts) are callable</done>
</task>

<task type="auto">
  <name>Task 2: Run scraper for AAPL and BRK.B to validate all 4 XTRCT requirements</name>
  <files>data/raw/AAPL/facts.json, data/raw/BRK.B/facts.json, data/cache/tickers.json</files>
  <action>
Run the scraper for AAPL and BRK.B to verify all success criteria. Execute from the project root.

**Step 1: Scrape AAPL**
```bash
cd "C:/Users/Seb/AI 2026" && python scraper.py AAPL
```
Expected: `data/raw/AAPL/facts.json` created (~3-8MB), log shows fiscal years available.

**Step 2: Run AAPL again (cache test — XTRCT-04)**
```bash
cd "C:/Users/Seb/AI 2026" && python scraper.py AAPL
```
Expected: "Using cached facts.json" log message appears, no SEC network call made.

**Step 3: Scrape BRK.B (special ticker test)**
```bash
cd "C:/Users/Seb/AI 2026" && python scraper.py BRK.B
```
Expected: `data/raw/BRK.B/facts.json` created. If BRK.B fails with ValueError, try BRK-B:
```bash
cd "C:/Users/Seb/AI 2026" && python scraper.py BRK-B
```
If BRK-B works but BRK.B doesn't, update the `resolve_cik()` fallback logic to also try replacing `-` with `.` (already in code) AND verify `company_tickers.json` actually has a BRK key by checking: `python -c "import json; d=json.load(open('data/cache/tickers.json')); print([v for v in d.values() if 'BRK' in v.get('ticker','')][:5])"`

**Step 4: Validate the facts.json structure (XTRCT-03)**
```bash
python -c "
import json, sys
sys.path.insert(0, 'C:/Users/Seb/AI 2026')
import scraper
from pathlib import Path
v = scraper.validate_facts(Path('C:/Users/Seb/AI 2026/data/raw/AAPL/facts.json'))
print('AAPL validation:', v)
assert v['ok'], 'facts.json validation failed'
assert v['fy_count'] >= 10, f'Expected 10+ fiscal years, got {v[\"fy_count\"]}'
print('All assertions passed')
"
```

**Step 5: Verify tickers.json local cache (XTRCT-01)**
```bash
python -c "
from pathlib import Path
import json
p = Path('C:/Users/Seb/AI 2026/data/cache/tickers.json')
assert p.exists(), 'tickers.json cache missing'
d = json.loads(p.read_text())
print(f'tickers.json: {len(d)} entries, sample key: {list(d.keys())[0]}')
"
```
  </action>
  <verify>
    <automated>python -c "
from pathlib import Path
import json, sys
sys.path.insert(0, 'C:/Users/Seb/AI 2026')
import scraper

base = Path('C:/Users/Seb/AI 2026')

# XTRCT-01: tickers.json exists locally
assert (base / 'data/cache/tickers.json').exists(), 'FAIL XTRCT-01: tickers.json missing'

# XTRCT-04: facts.json exists for AAPL
aapl_facts = base / 'data/raw/AAPL/facts.json'
assert aapl_facts.exists(), 'FAIL XTRCT-04: AAPL facts.json missing'

# XTRCT-03: validate 10-K data present
v = scraper.validate_facts(aapl_facts)
assert v['ok'], f'FAIL XTRCT-03: {v}'
assert v['fy_count'] >= 10, f'FAIL XTRCT-03: only {v[\"fy_count\"]} fiscal years (need 10+)'

print('XTRCT-01 PASS: tickers.json cached locally')
print(f'XTRCT-03 PASS: {v[\"fy_count\"]} fiscal years ({v[\"earliest_fy\"]}-{v[\"latest_fy\"]})')
print('XTRCT-04 PASS: facts.json exists')
print('XTRCT-02 PASS: rate_limit=8 set at module init (inspect scraper.py line with set_rate_limit)')
" 2>&1</automated>
    <manual>All 4 XTRCT PASS lines appear; fy_count is 10 or more for AAPL</manual>
  </verify>
  <done>
- data/raw/AAPL/facts.json exists with 10+ fiscal years of 10-K data
- data/cache/tickers.json exists (downloaded once, reused without network call)
- Second run of AAPL shows "Using cached facts.json" — no SEC re-fetch
- BRK.B (or BRK-B) scrapes successfully
- Validation script prints all 4 XTRCT PASS lines
  </done>
</task>

</tasks>

<verification>
Overall phase verification — run after both tasks complete:

```bash
python -c "
from pathlib import Path
import json, sys
sys.path.insert(0, 'C:/Users/Seb/AI 2026')
import scraper
from pathlib import Path

base = Path('C:/Users/Seb/AI 2026')

# Success Criterion 1: facts.json with 10 years
v = scraper.validate_facts(base / 'data/raw/AAPL/facts.json')
assert v['ok'] and v['fy_count'] >= 10, f'SC1 FAIL: {v}'
print(f'SC1 PASS: {v[\"fy_count\"]} fiscal years for AAPL')

# Success Criterion 2: tickers.json local cache
assert (base / 'data/cache/tickers.json').exists(), 'SC2 FAIL: tickers.json missing'
print('SC2 PASS: tickers.json cached locally (no per-resolution network call)')

# Success Criterion 3: rate limit configured
import inspect
src = inspect.getsource(scraper._init_edgar)
assert 'set_rate_limit(8)' in src, 'SC3 FAIL: rate limit not set'
print('SC3 PASS: set_rate_limit(8) in _init_edgar()')

# Success Criterion 4: cache-first logic in download_facts
src2 = inspect.getsource(scraper.download_facts)
assert 'out_path.exists()' in src2, 'SC4 FAIL: no cache check'
assert 'force_refresh' in src2, 'SC4 FAIL: no force_refresh param'
print('SC4 PASS: download_facts() checks existence before fetching')

print('--- ALL 4 SUCCESS CRITERIA PASSED ---')
"
```
</verification>

<success_criteria>
Phase 1 is complete when:
1. `python scraper.py AAPL` produces `data/raw/AAPL/facts.json` with 10+ fiscal years of 10-K facts
2. Running the scraper again for AAPL logs "Using cached facts.json" — no re-fetch
3. `data/cache/tickers.json` exists and is populated from one download, not per-ticker
4. `scraper._init_edgar()` source contains `set_rate_limit(8)`
5. `python scraper.py BRK.B` succeeds (or BRK-B with ticker normalization applied)
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-extraction/01-02-SUMMARY.md` with:
- Facts confirmed: AAPL fy_count, earliest_fy, latest_fy, file size
- BRK.B result: which ticker key resolved (BRK.B or BRK-B)
- Any deviations from the plan (e.g., import errors, version mismatches)
- Rate limit verified: set_rate_limit(8) confirmed in source
</output>
