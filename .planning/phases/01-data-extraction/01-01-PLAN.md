---
phase: 01-data-extraction
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - .env
  - data/cache/.gitkeep
  - data/raw/.gitkeep
autonomous: true
requirements:
  - XTRCT-01
  - XTRCT-02

must_haves:
  truths:
    - "Running `pip install -r requirements.txt` succeeds without dependency conflicts"
    - "A `.env` file exists with EDGAR_IDENTITY populated so the scraper can authenticate with SEC"
    - "The data/cache/ and data/raw/ directories exist so scraper.py can write without error"
  artifacts:
    - path: "requirements.txt"
      provides: "Pinned Python dependencies for Phase 1 scraper"
      contains: "edgartools"
    - path: ".env"
      provides: "EDGAR_IDENTITY secret for SEC User-Agent header"
      contains: "EDGAR_IDENTITY"
    - path: "data/cache/.gitkeep"
      provides: "Cache directory for tickers.json"
    - path: "data/raw/.gitkeep"
      provides: "Raw data directory for facts.json files"
  key_links:
    - from: ".env"
      to: "scraper.py"
      via: "python-dotenv load_dotenv()"
      pattern: "EDGAR_IDENTITY"
    - from: "requirements.txt"
      to: "scraper.py"
      via: "pip install"
      pattern: "edgartools>=5.0"
---

<objective>
Bootstrap the project dependencies and environment for the SEC EDGAR scraper.

Purpose: scraper.py (Plan 02) cannot run without installed packages and a valid EDGAR identity. This plan lays the foundation: pinned dependencies, a .env template with the real API key, and the data directory structure.
Output: requirements.txt, .env with EDGAR_IDENTITY set, data/ directory scaffold.
</objective>

<execution_context>
@C:/Users/Seb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Seb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-extraction/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create requirements.txt with pinned Phase 1 dependencies</name>
  <files>C:/Users/Seb/AI 2026/requirements.txt</files>
  <action>
Create `C:/Users/Seb/AI 2026/requirements.txt` with the following content exactly:

```
# Phase 1: Data Extraction
edgartools>=5.0
httpx>=0.27
tenacity>=8.3
python-dotenv>=1.0
loguru>=0.7
tqdm>=4.66

# Phase 2+ (add incrementally)
# pandas>=2.1
# pyarrow>=16.0
# duckdb>=1.0
# polars>=1.0
# numpy>=1.26
# streamlit>=1.35
# plotly>=5.22
# apscheduler>=3.10,<4.0
```

Then run `pip install -r "C:/Users/Seb/AI 2026/requirements.txt"` to install.

Note: edgartools is imported as `edgar` in Python code (e.g., `from edgar import set_identity`), not as `edgartools`. The package name and import name differ.
  </action>
  <verify>
    <automated>pip show edgartools httpx tenacity python-dotenv loguru tqdm 2>&1 | grep -E "^Name:|not found"</automated>
    <manual>All 6 packages appear in pip show output with no "not found" lines</manual>
  </verify>
  <done>pip show edgartools returns version 5.x or higher; all 6 Phase 1 packages installed without error</done>
</task>

<task type="auto">
  <name>Task 2: Create .env with EDGAR_IDENTITY and create data directory scaffold</name>
  <files>C:/Users/Seb/AI 2026/.env</files>
  <action>
Create `C:/Users/Seb/AI 2026/.env` with the following content:

```
# SEC EDGAR identity — required by SEC policy (User-Agent header)
# Format: "Full Name email@domain.com"
EDGAR_IDENTITY="Seb Analyst 752615f59034781aeb9c4e0613887fa159a63d30e8e50efc25011f745229c59b@sec-key.io"
```

Note: The SEC User-Agent format requires "Name email". The API key provided in the planning context is used as the email portion to uniquely identify this project's requests.

Then create the data directory scaffold using Python (not bash mkdir, to handle Windows paths correctly):

```python
from pathlib import Path
base = Path("C:/Users/Seb/AI 2026")
for d in ["data/raw", "data/cache", "data/clean"]:
    (base / d).mkdir(parents=True, exist_ok=True)
    (base / d / ".gitkeep").touch()
```

Run this as: `python -c "from pathlib import Path; base = Path('C:/Users/Seb/AI 2026'); [((base/d).mkdir(parents=True, exist_ok=True) or (base/d/'.gitkeep').touch()) for d in ['data/raw','data/cache','data/clean']]"`
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; p=Path('C:/Users/Seb/AI 2026'); print('env:', (p/'.env').exists()); print('raw:', (p/'data/raw').is_dir()); print('cache:', (p/'data/cache').is_dir()); print('clean:', (p/'data/clean').is_dir())"</automated>
    <manual>All four lines print True</manual>
  </verify>
  <done>.env exists with EDGAR_IDENTITY set; data/raw/, data/cache/, data/clean/ directories exist</done>
</task>

</tasks>

<verification>
After both tasks:
1. `pip show edgartools` returns Name: edgartools with version >= 5.0
2. `python -c "from dotenv import load_dotenv; import os; load_dotenv('C:/Users/Seb/AI 2026/.env'); print(bool(os.getenv('EDGAR_IDENTITY')))"` prints True
3. All three data/ subdirectories exist
</verification>

<success_criteria>
- requirements.txt exists at project root with all 6 Phase 1 packages
- All packages install cleanly (`pip install -r requirements.txt` exits 0)
- .env exists with a non-empty EDGAR_IDENTITY value in "Name email" format
- data/raw/, data/cache/, data/clean/ directories exist and are writable
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-extraction/01-01-SUMMARY.md` with:
- What was created (files, versions installed)
- Actual edgartools version installed (from pip show)
- Any install warnings or conflicts encountered
</output>
