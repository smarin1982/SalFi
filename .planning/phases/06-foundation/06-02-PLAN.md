---
phase: 06-foundation
plan: "02"
type: tdd
wave: 1
depends_on: []
files_modified:
  - company_registry.py
  - tests/test_company_registry.py
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03

must_haves:
  truths:
    - "make_slug('Clínica Las Américas') returns 'clinica-las-americas' — deterministic, ASCII-only"
    - "make_slug('EPS Sánitas (NUEVA)') returns 'eps-sanitas-nueva' — parentheses stripped"
    - "Same company name always produces the same slug across multiple calls"
    - "Slug-derived paths can be created on Windows NTFS without OSError"
    - "CompanyRecord stores regulatory_id (NIT/RUC/RUT) as a field"
    - "data/latam/{country}/{slug}/ directory structure created by make_storage_path()"
    - "financials.parquet schema matches exactly: 24 columns, same names and dtypes as data/clean/AAPL/financials.parquet"
    - "meta.json written with correct structure including low_confidence_fx flag"
  artifacts:
    - path: "company_registry.py"
      provides: "CompanyRecord dataclass, make_slug(), make_storage_path(), write_meta_json()"
      exports: ["CompanyRecord", "make_slug", "make_storage_path", "write_meta_json"]
      min_lines: 80
    - path: "tests/test_company_registry.py"
      provides: "Test suite for slug generation, storage layout, Parquet schema parity"
      min_lines: 70
  key_links:
    - from: "company_registry.py"
      to: "python-slugify"
      via: "from slugify import slugify with allow_unicode=False"
      pattern: "slugify.*allow_unicode=False"
    - from: "company_registry.py"
      to: "data/latam/{country}/{slug}/"
      via: "make_storage_path() using pathlib.Path.mkdir(parents=True, exist_ok=True)"
      pattern: "mkdir.*parents=True"
    - from: "company_registry.py"
      to: "data/latam/{country}/{slug}/meta.json"
      via: "write_meta_json() writing dataclass as JSON"
      pattern: "meta\\.json"
---

<objective>
Implement the company registry (company_registry.py) using TDD — test first, then implement. Covers slug generation from Unicode company names, storage path creation, regulatory ID storage, and Parquet schema parity validation.

Purpose: The slug is the permanent filesystem key for every LATAM company. It must be deterministic, Windows-safe, and generated from Unicode Spanish/Portuguese names. The storage layout must exactly mirror the US pipeline schema so that existing dashboard code works without modification.

Output: company_registry.py (fully implemented), tests/test_company_registry.py (all green), data/latam/ directory structure ready for use by Phase 7+.
</objective>

<execution_context>
@C:/Users/Seb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Seb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-foundation/06-RESEARCH.md
@requirements.txt
</context>

<feature>
  <name>Company Registry — company_registry.py</name>
  <files>company_registry.py, tests/test_company_registry.py</files>
  <behavior>
    Public API:
      make_slug(company_name: str) -> str
        - "Clínica Las Américas" → "clinica-las-americas"
        - "EPS Sánitas (NUEVA)" → "eps-sanitas-nueva"
        - "Organización Sanitas S.A." → "organizacion-sanitas-s-a"
        - Always lowercase, hyphen-separated, ASCII only, no special chars
        - Deterministic: same input → same output always

      make_storage_path(base_dir: Path, country: str, slug: str) -> Path
        - Returns base_dir / "latam" / country.lower() / slug
        - Creates directories with mkdir(parents=True, exist_ok=True)
        - "colombia" → "colombia" (lowercase enforced)

      write_meta_json(path: Path, record: CompanyRecord) -> None
        - Writes meta.json to path / "meta.json"
        - JSON matches schema from RESEARCH.md Pattern 3 (meta.json schema)

      CompanyRecord dataclass fields:
        company_name: str (full Unicode display name)
        slug: str (ASCII, generated by make_slug)
        country: str (lowercase country name)
        regulatory_id: str (NIT/RUC/RUT value)
        regulatory_authority: str (e.g. "Supersalud")
        source_url: str
        currency_original: str
        approximated_fx: bool = False
        low_confidence_fx: bool = False

    Cases (test inputs → expected):
      make_slug("Clínica Las Américas") → "clinica-las-americas"
      make_slug("EPS Sánitas (NUEVA)") → "eps-sanitas-nueva"
      make_slug("Organización Sanitas S.A.") → "organizacion-sanitas-s-a"
      make_slug("SAME INPUT") called twice → same string both times
      make_storage_path(Path("data"), "Colombia", "grupo-keralty") →
        creates data/latam/colombia/grupo-keralty/ and returns the Path
      Path derived from make_slug() → no OSError on Windows when used in mkdir
      CompanyRecord(regulatory_id="NIT 800.058.016-0", ...) → regulatory_id stored correctly
      write_meta_json() → meta.json file contains "regulatory_id", "low_confidence_fx", "approximated_fx"
      Parquet schema parity: data/clean/AAPL/financials.parquet columns list matches EXPECTED_FINANCIALS_COLS
  </behavior>
  <implementation>
    Follow RESEARCH.md Patterns 2 and 4:

    1. Create tests/test_company_registry.py (RED phase) with these test functions:
       - test_slug_with_accents(): make_slug("Clínica Las Américas") == "clinica-las-americas"
       - test_slug_with_parens(): make_slug("EPS Sánitas (NUEVA)") == "eps-sanitas-nueva"
       - test_slug_with_period(): make_slug("Organización Sanitas S.A.") — assert result is str, no dots or parens
       - test_slug_deterministic(): call make_slug("TEST Ñoño") twice, assert both calls return same string
       - test_slug_windows_path(): slug = make_slug("Clínica Las Américas"); path = Path(tempfile.mkdtemp()) / slug; path.mkdir(); assert path.exists()
       - test_regulatory_id_stored(): CompanyRecord(company_name="Test", slug="test", country="colombia",
           regulatory_id="NIT 123", regulatory_authority="Supersalud", source_url="http://x.com",
           currency_original="COP").regulatory_id == "NIT 123"
       - test_make_storage_path(): path = make_storage_path(Path(tmp_dir), "Colombia", "grupo-keralty");
           assert path.exists(); assert "latam/colombia/grupo-keralty" in path.as_posix()
       - test_write_meta_json(): create a CompanyRecord, call write_meta_json(path, record),
           read back JSON, assert keys "company_name", "slug", "regulatory_id", "low_confidence_fx" present
       - test_ars_low_confidence_in_meta(): CompanyRecord with currency_original="ARS", low_confidence_fx=True;
           write_meta_json; read back; assert low_confidence_fx is True
       - test_parquet_schema_parity(): read data/clean/AAPL/financials.parquet, compare list(df.columns)
           against EXPECTED_FINANCIALS_COLS (24 columns, exact order, from RESEARCH.md).
           This test validates the schema we must replicate — it should PASS even in RED phase.

    2. Run tests (RED phase — all except test_parquet_schema_parity should fail with ImportError):
       python -m pytest tests/test_company_registry.py -v --tb=short

    3. Implement company_registry.py:
       - Import: from dataclasses import dataclass, field; from pathlib import Path; import json
       - Import: from slugify import slugify  # python-slugify — already installed by Plan 01
       - Define EXPECTED_FINANCIALS_COLS list (24 columns, exact order from RESEARCH.md)
       - Define EXPECTED_KPIS_COLS list (22 columns, exact order from RESEARCH.md)
       - @dataclass CompanyRecord with all 9 fields listed in behavior section
         Use field(default=False) for approximated_fx and low_confidence_fx
       - def make_slug(company_name: str) -> str:
           return slugify(company_name, allow_unicode=False, separator="-")
           # allow_unicode=False forces ASCII transliteration — CRITICAL for Windows NTFS safety
           # Do NOT use allow_unicode=True (see RESEARCH.md Pitfall 4)
       - def make_storage_path(base_dir: Path, country: str, slug: str) -> Path:
           path = base_dir / "latam" / country.lower() / slug
           path.mkdir(parents=True, exist_ok=True)
           return path
       - def write_meta_json(path: Path, record: CompanyRecord) -> None:
           meta = {
             "company_name": record.company_name,
             "slug": record.slug,
             "country": record.country,
             "regulatory_id": record.regulatory_id,
             "regulatory_authority": record.regulatory_authority,
             "source_url": record.source_url,
             "currency_original": record.currency_original,
             "approximated_fx": record.approximated_fx,
             "low_confidence_fx": record.low_confidence_fx,
             "last_scraped": None,
             "last_processed": None,
             "fiscal_years_available": [],
             "extraction_quality": None,
             "fields_extracted": [],
             "fields_missing": []
           }
           (path / "meta.json").write_text(json.dumps(meta, indent=2, ensure_ascii=False))
         # ensure_ascii=False preserves Unicode in display names stored in meta.json

    4. Run tests (GREEN phase — all MUST pass):
       python -m pytest tests/test_company_registry.py -v

    5. Run full suite for regression check:
       python -m pytest tests/ -v
  </implementation>
</feature>

<verification>
  <automated>python -m pytest tests/test_company_registry.py -v</automated>
  <manual>Confirm that running the tests creates a data/latam/ directory under the temp dir used in test_make_storage_path (temp dir is cleaned up by tempfile). Verify test_parquet_schema_parity passes — this confirms the US Parquet schema is still intact and we know what to replicate.</manual>
  <sampling_rate>Run after GREEN phase commit. Run full suite before Plan 03.</sampling_rate>
</verification>

<success_criteria>
- All 10 tests in tests/test_company_registry.py pass (pytest exit 0)
- company_registry.py exists at project root with make_slug(), make_storage_path(), write_meta_json(), CompanyRecord exported
- make_slug("Clínica Las Américas") returns "clinica-las-americas" (verified by test)
- test_parquet_schema_parity passes — US schema confirmed intact for parity reference
- Full test suite python -m pytest tests/ -v shows no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/06-foundation/06-02-SUMMARY.md` following the summary template.
</output>
